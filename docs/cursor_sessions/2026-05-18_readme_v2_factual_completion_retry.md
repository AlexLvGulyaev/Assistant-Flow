# Cursor task: README v2 factual completion and polishing

Файл задачи: `cursor_tasks_local/2026-05-18_readme_v2_factual_completion.md`

## Контекст

В корневом каталоге проекта размещён новый draft README v2 без переименования. Нужно доработать **только фактические и технические разделы**, помеченные как TODO / требующие проверки, а также привести ссылки на скриншоты и структуру проекта в соответствие с фактическим состоянием репозитория.

README уже прошёл содержательную редактуру у оператора и архитектора. Не надо переписывать его с нуля и не надо менять общий стиль документа.

## Главная цель

Подготовить README к следующему review-pass:

- проверить фактуру;
- убрать TODO, которые можно закрыть по репозиторию;
- актуализировать структуру проекта;
- проверить пути к скриншотам;
- уточнить раздел развёртывания;
- уточнить `.env` / переменные окружения;
- проверить названия разделов Admin UI;
- не сломать русскоязычный стиль README.

## Жёсткие ограничения

1. Не переписывать README целиком.
2. Не менять бизнес-сценарий и продуктовую подачу без явной необходимости.
3. Не добавлять агрессивные противопоставления вида:
   - «это не учебная демка, а настоящая система»;
   - «не просто бот, а enterprise-платформа»;
   - «в отличие от обычных RAG-ботов».
4. Не превращать README в runbook или architecture spec.
5. Не перегружать текст англоязычными терминами.
6. Не показывать в публичной структуре проекта `cursor_tasks_local/`.
7. Не показывать внутренние временные файлы, локальные артефакты, кэши, node_modules, dist, runtime outputs.
8. Не менять названия скриншотов без необходимости.
9. Не выполнять rebuild/redeploy.
10. Не менять код приложения, если это не требуется для проверки README.

## Стиль языка

README должен быть написан преимущественно на нормальном русском инженерном языке.

Допустимые термины, которые можно оставить:

- AI;
- RAG;
- retrieval;
- backend;
- chunk / чанки;
- embedding / эмбеддинги;
- API;
- LLM;
- STT / TTS;
- RAGAS;
- Docker;
- FastAPI;
- React;
- PostgreSQL;
- Chroma / FAISS / Weaviate.

Но избегать англо-русских гибридов там, где есть нормальная русская формулировка.

Предпочтительные замены:

| Плохо | Лучше |
|---|---|
| text pipeline | текстовый AI-контур / контур текстовой обработки |
| runtime logging | техническое логирование |
| admin observability | операционная наблюдаемость |
| operational UX | операторский интерфейс |
| multi-LLM routing | маршрутизация между AI-провайдерами |
| storage layer | контур хранения данных |
| execution tracing | трассировка обработки запросов |
| retrieval diagnostics | диагностика поиска по базе знаний |
| cache lifecycle | жизненный цикл кэша |
| startup sequence | порядок запуска |

## Что нужно проверить и доработать

### 1. README-файл

Найти файл draft README v2 в корневом каталоге проекта.

Если он называется не `README.md`, не переименовывать автоматически без явного решения оператора. В отчёте указать фактическое имя файла.

### 2. Скриншоты

Проверить, что в каталоге `screenshots` или согласованном каталоге скриншотов реально существуют файлы:

- `audio-adm.png`
- `audio-tg.png`
- `documents-adm.png`
- `image-adm.png`
- `image-tg.png`
- `logs-adm.png`
- `mem-adm.png`
- `overview-adm.png`
- `rag-adm.png`
- `ragas-adm.png`
- `rag-tg.png`
- `rs-adm.png`
- `summary-adm.png`
- `text-adm.png`
- `text-tg.png`

Если фактический каталог не `screenshots`, а например `docs/screenshots`, обновить пути в README и явно указать это в отчёте.

### 3. Названия разделов Admin UI

Проверить по коду `frontend/admin-ui` фактические названия пунктов меню / страниц.

Особенно сверить:

- Обзор
- Сводка
- Текст
- RAG
- Изображения
- Аудио
- Документы
- Retrieval Settings
- Логи
- Memory
- Анализ RAG

Если в UI используются другие русские названия, исправить README под фактические названия.

Если название в UI пока англоязычное, не придумывать перевод в README без основания: указать фактическое название.

### 4. Структура проекта

Актуализировать раздел `Структура проекта`.

Показать только публично полезные рабочие каталоги.

Обязательно проверить наличие и назначение:

- `admin_api/`
- `admin_ui/` если ещё есть как legacy / исторический контур
- `core/`
- `providers/`
- `services/`
- `interfaces/`
- `repositories/`
- `database/`
- `storage/` если это не runtime-only и уместно для README
- `outputs/` если не должен попадать в публичную структуру — не показывать или указать осторожно
- `frontend/`
- `docs/`
- `evaluation/`
- `scripts/`
- `utils/`
- `PROJECT_STATE.md`
- `docker-compose.portfolio.yml`
- `docker-compose.assistant.yml`

Не включать:

- `cursor_tasks_local/`
- `.venv/`
- `node_modules/`
- `dist/`
- временные кэши;
- локальные runtime-артефакты;
- приватные файлы окружения.

### 5. Развёртывание

Доработать раздел `Развертывание`.

Нужно проверить и указать:

- какая compose-команда является основной для portfolio-контура;
- нужно ли использовать `-p portfolio-test`;
- нужен ли `COMPOSE_BAKE=false`;
- нужен ли `--build`;
- нужен ли `--remove-orphans`;
- какие порты реально открываются:
  - Admin UI;
  - Admin API;
  - PostgreSQL;
  - Chroma;
  - Weaviate, если используется;
- какие сервисы должны подняться;
- какая команда проверки после запуска.

Важно: если текущая каноническая команда в проекте такая:

```bash
COMPOSE_BAKE=false docker compose -p portfolio-test -f docker-compose.portfolio.yml up -d --build
```

то именно её и нужно использовать в README для portfolio-контура.

Не писать обобщённо `docker compose up -d --build`, если это может поднять параллельный неправильный контур.

### 6. Переменные окружения

Доработать раздел `.env` / конфигурации.

Нужно указать:

- что копируется `.env.example`;
- какие ключи обязательны для живого Telegram-бота;
- какие ключи нужны для LLM / embeddings / images / audio;
- какие переменные относятся к Chroma / Weaviate / FAISS;
- какие параметры относятся к retrieval cache;
- что плейсхолдер Telegram-токена оставляет бота в режиме ожидания, если так действительно реализовано.

Не вставлять реальные секреты.

### 7. Retrieval cache

Проверить, что раздел про кэширование retrieval-запросов соответствует текущей реализации.

Уточнить формулировки, если нужно:

- OFF;
- MISS;
- HIT;
- cache latency;
- retrieval latency;
- generation / revision;
- TTL;
- backend fingerprint;
- сброс при reindex / смене backend / смене top_k / bump generation.

Не перегружать раздел деталями runbook. README должен объяснять возможность и показывать ценность.

### 8. Статус подсистем

Проверить раздел `Текущий статус проекта`.

Если утверждения слишком сильные или не подтверждаются кодом — смягчить.

Например:

- «стабильные подсистемы» оставить только для реально работающих;
- спорные элементы перенести в «активно развиваются»;
- experimental элементы пометить аккуратно.

### 9. Документация проекта

Проверить наличие или планируемое наличие:

- `ARCHITECTURE.md`
- `RUNBOOK.md`
- `USER_GUIDE.md`
- `PROJECT_STATE.md`

Если каких-то файлов пока нет, не писать так, будто они уже есть. Можно использовать формулировку:

- «основные документы проекта» — если существуют;
- «документы, готовящиеся к GitHub v2.0» — если часть ещё в работе.

Выбрать формулировку строго по фактическому состоянию.

### 10. Финальный markdown-polish

Проверить:

- корректность заголовков;
- относительные ссылки;
- пути к изображениям;
- отсутствие битых ссылок;
- отсутствие лишних TODO, если они уже закрыты;
- отсутствие дублирующихся разделов;
- аккуратность таблиц;
- отсутствие случайных внутренних комментариев, которые не должны попасть в GitHub.

## Что НЕ делать

Не нужно:

- писать новый README с нуля;
- менять продуктовую идею;
- переписывать бизнес-сценарий;
- делать новый дизайн документа;
- создавать новые скриншоты;
- менять код приложения;
- запускать дорогие тесты;
- делать full rebuild;
- выполнять git commit без указания оператора.

## Ожидаемый результат

1. Обновить README draft в корне проекта.
2. Убрать или закрыть TODO, которые можно закрыть фактической проверкой.
3. Оставшиеся TODO оставить только если без оператора их нельзя закрыть.
4. В конце task-файла или в отдельном session log указать:
   - какие файлы изменены;
   - какие разделы README были доработаны;
   - какие TODO закрыты;
   - какие вопросы остались к оператору;
   - какие команды были выполнены для проверки;
   - нужны ли дополнительные скриншоты.

## Verification commands

Выполнить безопасные проверки:

```bash
git status --short
find . -maxdepth 3 -type f | sort | sed 's#^./##' | head -200
find . -type f \( -path "*/screenshots/*" -o -path "*/docs/screenshots/*" \) | sort
```

Если затронут frontend paths / названия страниц, можно выполнить только статическую проверку файлов:

```bash
find frontend/admin-ui -maxdepth 4 -type f | sort | head -200
```

Если README ссылается на compose, проверить наличие файлов:

```bash
ls -la docker-compose*.yml .env.example 2>/dev/null || true
```

Не запускать rebuild без отдельного разрешения оператора.

## Важное требование к отчёту Cursor

В отчёте обязательно написать:

```text
## Operator commands / next verification commands
```

И указать только реальные следующие команды, если они нужны.

Если README изменён, но пересборка не нужна, так и написать:

```text
Rebuild не требуется: изменена только документация.
```

---

## Отчёт Cursor (2026-05-18)

### Изменённые файлы

- `readme_v_2_draft_for_cursor.md` — единственный правленный артефакт (draft в корне, **не** переименован в `README.md`).

### Доработанные разделы

| Раздел | Действие |
|--------|----------|
| Скриншоты | `screenshots/` → `docs/screenshots/` (15 файлов проверены) |
| Операционная консоль | таблица пунктов меню из `frontend/admin-ui/src/navigation/routes.ts` |
| Структура проекта | актуализировано дерево, без `cursor_tasks_local/`, `outputs/` |
| Развёртывание | portfolio: `COMPOSE_BAKE=false`, `-p portfolio-test`, порты, health check |
| `.env` | таблицы по группам переменных, placeholder Telegram |
| Кэш retrieval | TTL, generation, сравнение сессий, RS vs env |
| Статус | смягчён блок «активно развиваются» |
| Документация | `docs/ARCHITECTURE.md`, `docs/OPERATIONS.md`; нет RUNBOOK/USER_GUIDE в корне |
| Инфраструктура | Traefik только для server-compose |

### Закрытые TODO

- структура каталогов;
- compose / порты / startup;
- ENV configuration;
- пути скриншотов.

### Вопросы к оператору

1. Переименовать `readme_v_2_draft_for_cursor.md` → `README.md` (заменить текущий) при review?
2. Нужен ли отдельный `RUNBOOK.md` / `USER_GUIDE.md` для GitHub v2.0?
3. Показывать ли `legacy/` в публичном README (сейчас одна строка «учебный reference»).

### Команды проверки

```bash
git status --short
find docs/screenshots -type f | sort
ls -la docker-compose*.yml .env.example
```

### Operator commands / next verification commands

```text
Rebuild не требуется: изменена только документация.

# Просмотр draft
less readme_v_2_draft_for_cursor.md

# Проверка ссылок на скриншоты в браузере/GitHub preview
# При принятии draft — заменить README.md вручную или по отдельной задаче
```
