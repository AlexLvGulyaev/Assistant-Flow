# Промпт для Cursor: обновление PROJECT_STATE.md по консоли «Анализ качества RAG»

```text
Используй Codex 5.3.

Нужно обновить PROJECT_STATE.md по итогам реализации подсистемы «Анализ качества RAG» в Assistant Flow.

ВАЖНО:
- язык общения, комментариев и отчётов — русский;
- язык изменений в PROJECT_STATE.md — русский;
- не переписывай PROJECT_STATE.md целиком;
- вноси только подтверждённые изменения;
- не придумывай отсутствующий функционал;
- сохрани стиль существующего PROJECT_STATE.md;
- все изменения обязательно зафиксируй в session log;
- commit НЕ выполнять.

==================================================
ЦЕЛЬ
==================================================

Зафиксировать в PROJECT_STATE.md текущее состояние проекта после внедрения консоли «Анализ качества RAG».

Нужно описать не только сам экран, но и связанные с ним архитектурные, операционные и исследовательские выводы.

==================================================
ЧТО НУЖНО ОТРАЗИТЬ
==================================================

1. Консоль «Анализ качества RAG»

Добавить информацию о том, что в Admin UI появился отдельный раздел:

- «Анализ качества RAG»;
- импорт RAG-сессий из operational logs;
- запуск оценки RAGAS из интерфейса;
- просмотр найденных retrieval-чанков;
- ручное заполнение и проверка эталонных ответов;
- поддержка именованных наборов анализа;
- хранение результатов анализа в PostgreSQL;
- просмотр метрик по каждой RAG-сессии;
- сопоставление вопроса, ответа системы, эталона, чанков и метрик.

Не писать, что это финальный промышленный модуль.
Корректная формулировка:
- operational prototype;
- исследовательско-диагностический контур;
- часть Admin UI для анализа качества RAG.

2. Диагностика retrieval и качества ответов

Отразить, что по результатам экспериментов были выявлены:

- шум в найденных retrieval-контекстах;
- зависимость context_precision от позиции релевантного чанка;
- влияние порядка чанков на итоговую оценку RAGAS;
- случаи, когда ответ правильный, но не подтверждён текущим retrieval;
- влияние истории диалога на ответ при нулевой поддержке текущим retrieval;
- нестабильность RAGAS при оценке negative-answer кейсов;
- необходимость ручной интерпретации автоматических метрик.

ВАЖНО:
Не использовать термин memory leakage как факт.
Корректная формулировка:
«влияние истории диалога на ответ при отсутствии поддержки текущим retrieval».

3. Эксперименты с параметрами retrieval

Зафиксировать проведённые сравнения:

- baseline: chunk_size=1000, overlap=200, top_k=5, backend Weaviate;
- второй эксперимент: chunk_size=1000, overlap=100, top_k=3, backend Weaviate;
- эксперимент с chunk_size=300, overlap=50, top_k=3 показал ухудшение recall для части factual-вопросов.

Основной вывод:
- top_k=3 на данном наборе данных дал более чистый retrieval-контекст, чем top_k=5;
- чрезмерное уменьшение размера чанка может ухудшать полноту поиска;
- оптимальные параметры зависят от структуры документов и должны проверяться экспериментально.

4. Архитектурные и операционные решения

Добавить, что:

- RAGAS-интеграция выполняется в admin-api runtime;
- зависимости RAGAS устанавливаются в admin-api образ через отдельный build path;
- результаты анализа сохраняются в PostgreSQL;
- evaluation workflow стал частью операционной диагностики Assistant Flow;
- session logs используются для фиксации задач Cursor и результатов внедрения;
- консоль анализа качества дополняет RAG-консоль и Logs-консоль.

5. UI / UX

Отразить:

- раздел Evaluation переименован в «Анализ RAG» в меню;
- страница называется «Анализ качества RAG»;
- интерфейс русифицирован;
- добавлены именованные наборы анализа;
- улучшено поведение прокрутки;
- карточка набора получила заголовок вида «Анализ набора: <имя набора>»;
- экран приведён к стилю остальных operational-консолей Assistant Flow.

6. Основные выводы исследования

Кратко зафиксировать:

- answer_relevancy не использует GT напрямую и может быть высокой у фактически неверного ответа;
- faithfulness проверяет подтверждение ответа текущими retrieval-чанками;
- context_precision чувствительна к позиции релевантного чанка;
- большие retrieval-контексты могут увеличивать шум;
- автоматические оценки RAGAS полезны, но требуют ручной проверки;
- для production-подхода нужны reranking, диагностика retrieval и ручная проверка спорных кейсов.

==================================================
SESSION LOGGING
==================================================

Создай session log:

`docs/cursor_sessions/2026-05-16_project_state_rag_quality_analysis_update.md`

В начало session log полностью помести этот prompt.

В конец добавь:
1. какие разделы PROJECT_STATE.md изменены;
2. краткое описание внесённых изменений;
3. что НЕ менялось;
4. команды проверки;
5. git status.

==================================================
ОТВЕТ
==================================================

В ответе предоставить только:
1. список изменённых файлов;
2. список изменённых разделов PROJECT_STATE.md;
3. краткое summary;
4. git status.
```

## Выполнение

### 1) Какие разделы PROJECT_STATE.md изменены
- Добавлен новый append-only раздел:
  - `## 55. Консоль «Анализ качества RAG» — operational prototype (append-only)`
- Внутри него добавлены подпункты:
  - `55.1 Статус подсистемы`
  - `55.2 Функциональные возможности контура анализа`
  - `55.3 Хранение и runtime-контур`
  - `55.4 Диагностика retrieval и качества ответов (выводы по экспериментам)`
  - `55.5 Сравнение retrieval-параметров (операционные эксперименты)`
  - `55.6 UI/UX состояние контура`
  - `55.7 Исследовательские выводы по метрикам`

### 2) Краткое описание внесённых изменений
- Зафиксировано текущее состояние operational-консоли «Анализ качества RAG» в Admin UI.
- Добавлены подтвержденные возможности: импорт RAG-сессий, запуск RAGAS из UI, просмотр чанков, ручной GT/review, именованные наборы анализа, связка question/answer/GT/chunks/metrics.
- Зафиксированы операционные и исследовательские выводы по retrieval/RAGAS:
  - шум контекста,
  - чувствительность `context_precision` к позиции чанка,
  - влияние порядка чанков,
  - влияние истории диалога при отсутствии поддержки текущим retrieval,
  - нестабильность negative-answer оценок,
  - необходимость ручной интерпретации метрик.
- Добавлены итоги экспериментов параметров (`1000/200/k5`, `1000/100/k3`, `300/50/k3`) и выводы о trade-off между чистотой контекста и recall.
- Отражены архитектурные решения: RAGAS в `admin-api` runtime, отдельный build path зависимостей RAGAS, сохранение результатов в PostgreSQL, роль session logs.
- Отражены UI/UX-изменения: `Evaluation -> Анализ RAG`, заголовок страницы, русификация, именованные наборы, scroll UX, заголовок вида `Анализ набора: <имя>`.

### 3) Что НЕ менялось
- API и схемы данных.
- RAGAS/retrieval runtime-логика.
- Логика импорта и именования наборов в БД.
- Существующие разделы `PROJECT_STATE.md` вне append-only добавления.

### 4) Команды проверки
- `git status --short`

### 5) Git status
```text
 M PROJECT_STATE.md
```
